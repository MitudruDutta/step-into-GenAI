[project]
name = "mcp-server"
version = "0.1.0"
description = "A Model Context Protocol (MCP) server for employee leave management, providing tools to check balances, apply leave, and view history."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["mcp"]
